---
all:
  children:
    greener_vps:
      hosts:
        greener-prod:
          # VPN-only: ansible_host is the VPS address on the OpenVPN network.
          ansible_host: 51.255.169.129  # <-- set the real prod VPN IP (ATM public IP only)
