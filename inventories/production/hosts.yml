---
all:
  children:
    greener_vps:
      hosts:
        greener-prod:
          # Bootstrap public IP: OpenVPN not yet deployed (VPN is a stub), so the public
          # IP is the only working access today. Switch to the VPN IP once SCRUM-58
          # (OpenVPN) is deployed and VPN-only SSH hardening is active.
          ansible_host: 51.255.169.129
