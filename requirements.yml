---
# Pinned collections. Install with: make deps (ansible-galaxy collection install).
collections:
  - name: community.general
    version: "9.5.0"
  - name: ansible.posix
    version: "1.6.2"
  - name: community.docker
    version: "3.13.0"
