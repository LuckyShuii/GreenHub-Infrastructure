# `ssh` role

**SSH service hardening (intended to be reachable via VPN only, once OpenVPN is deployed).**

> Foundation stub (SCRUM-60). Structure is complete and conventional; tasks will
> be implemented in a dedicated ticket. Holds no secrets.
