---
# Reload, never restart: restarting dockerd stops every container on the host, application
# included, for a change that only affects containers created afterwards anyway.
- name: Reload docker
  ansible.builtin.systemd:
    name: docker
    state: reloaded
